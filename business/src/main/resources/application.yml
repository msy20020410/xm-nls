#spring.application.name=business

server:
  port: 18000  # 端口号
  servlet:
    context-path: /nls  # 上下文路径

# 数据源相关配置
spring:
  datasource:
    url: jdbc:mysql://124.222.138.172:33060/xm_nls?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  # 指定 Mapper.xml 文件路径
  mapper-locations: classpath:mapper/**/*.xml
  # 指定实体类别名包路径
  type-aliases-package: com.xm.nls.business.model.entity
  # 开启驼峰命名转换
  configuration:
    map-underscore-to-camel-case: true

# 开启持久层日志
logging:
  level:
    com.xm.nls.business.mapper: trace


